# robots.txt de aurin.mx
#
# Un solo grupo abierto. Los grupos por bot que había antes (GPTBot,
# ClaudeBot, PerplexityBot, Googlebot, Bingbot…) repetían exactamente lo que
# ya dice `User-agent: *`, y cada bot obedece únicamente al grupo más
# específico que lo nombra: mantenerlos solo abría la puerta a que una regla
# futura en `*` se les olvidara. Si algún día hay que bloquear un crawler,
# se le agrega su propio grupo con el Disallow que corresponda.

User-agent: *
Allow: /

Sitemap: https://aurin.mx/sitemap.xml
